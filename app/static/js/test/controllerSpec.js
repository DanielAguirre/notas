describe('NoteController', function(){

  beforeEach(module('notas'));

  it('should create "phones" model with 3 phones', (function($controller) {
    /*var scope = {},
        ctrl = $controller('NotaController', {$scope:scope});

   expect(scope.phones.length).toBe(3);**/
  }));

});